<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.shape.Circle?>
<?import javafx.scene.shape.*?>
<?import javafx.geometry.Insets?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.image.Image?>

<VBox alignment="CENTER" spacing="10" styleClass="card" xmlns="http://javafx.com/javafx/17" xmlns:fx="http://javafx.com/fxml/1" fx:controller="com.focusnode.controller.PomodoroCardController">
    <HBox alignment="CENTER_LEFT" spacing="8">
        <ImageView fitHeight="18" fitWidth="18" pickOnBounds="true" preserveRatio="true">
            <image><Image url="@/images/timer.png" /></image>
        </ImageView>
        <Label text="Pomodoro" styleClass="card-title" />
    </HBox>

    <!-- Vòng tròn Timer -->
    <StackPane>
        <Circle radius="70" fill="TRANSPARENT" stroke="#E2E8F0" strokeWidth="8" />
        <!-- Vòng cung thể hiện tiến độ (Dùng Arc trong code logic, ở đây dùng Circle tạm) -->
        <Circle radius="70" fill="TRANSPARENT" stroke="#10B981" strokeWidth="8" strokeDashArray="100, 400" />
        
        <VBox alignment="CENTER">
            <Label fx:id="timeLabel" text="25:00" style="-fx-font-size: 28px; -fx-font-weight: bold; -fx-text-fill: #1E293B;" />
            <Label text="Focus Time" style="-fx-font-size: 12px; -fx-text-fill: #64748B;" />
        </VBox>
    </StackPane>

    <!-- Nút điều khiển -->
    <VBox spacing="10" alignment="CENTER" maxWidth="Infinity">
        <Button text="▶ Start Focus" onAction="#startFocus" maxWidth="Infinity" styleClass="btn-primary" />
        <Button text="☕ Short Break" onAction="#startBreak" maxWidth="Infinity" styleClass="btn-text" />
    </VBox>
</VBox>